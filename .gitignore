node_modules
.DS_Store
dist
dist-ssr
*.local
.env
.nitro
.tanstack
.wrangler
.output
.vinxi
__unconfig*
todos.json

# local file-backed "leads" store used by the contact form demo —
# in production this server function would write to a real database instead.
.data/
